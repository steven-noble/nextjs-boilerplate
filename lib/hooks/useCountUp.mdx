import { Meta } from '@storybook/addon-docs'

<Meta title="Developers/Hooks/useCountUp" />

# useCountUp

```
import useCountUp from '@lib/hooks/useCountUp';

const counter = useCountUp(0, 50, 2000) 
```

Accepts start, end and duration