import { Meta } from '@storybook/addon-docs'

<Meta
    title="Developers/Hooks/useCardRatio"
/>

# useCardRatio (WIP)

```
import { useCardRatio } from '@hooks/useCardRatio'

const [aspectRatio, calculateRatio] = useCardRatio(1.586)
```
